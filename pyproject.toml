[project]
name = "real-decision-agent"
version = "0.1.0"
description = "Mobile-first REAL decision-support agent."
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

